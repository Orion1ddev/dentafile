{
  "build": {
    "command": "npm install && npm run build",
    "directory": "dist",
    "environment": {
      "NODE_VERSION": "18.18.0"
    }
  },
  "routes": [
    { "pattern": "/*", "dest": "/index.html" }
  ]
} 
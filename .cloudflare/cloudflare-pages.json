{
  "build": {
    "command": "npm ci && npm run build",
    "directory": "dist",
    "environment": {
      "NODE_VERSION": "18.18.0",
      "NPM_FLAGS": "--no-audit --no-fund"
    }
  },
  "routes": [
    { "pattern": "/*", "dest": "/index.html" }
  ]
} 